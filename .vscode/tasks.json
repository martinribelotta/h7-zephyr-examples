{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "west build",
            "type": "shell",
            "command": "west build blinky -b generic_h750vb -DBOARD_ROOT=${workspaceFolder}"
        },
        {
            "label": "west clean",
            "type": "shell",
            "command": "west clean blinky -b generic_h750vb -DBOARD_ROOT=${workspaceFolder}"
        },
        {
            "label": "west flash",
            "type": "shell",
            "command": "west flash"
        }
    ]
}